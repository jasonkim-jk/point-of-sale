import React from 'react';

export default class ViewRestaurant extends React.Component {
  componentDidMount() {
    // run fetch inside component did mount
    // inside fetch , set state
    // in render map over state
    //
  }
}

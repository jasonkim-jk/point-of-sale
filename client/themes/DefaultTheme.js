import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#1dbe94',
      contrastText: '#fff'
    },
    secondary: {
      main: '#ef476f'
    }
  }
});

export default theme;
